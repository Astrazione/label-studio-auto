services:
  label-studio:
    image: heartexlabs/label-studio:latest
    container_name: label-studio
    restart: unless-stopped
    expose:
      - "8080"
    volumes:
      - ./label-studio-data:/label-studio/data
    environment:
      LABEL_STUDIO_HOST: https://label-studio-auto.twilightparadox.com
      CSRF_TRUSTED_ORIGINS: https://label-studio-auto.twilightparadox.com
      LABEL_STUDIO_LOCAL_FILES_SERVING_ENABLED: "true"
      LABEL_STUDIO_LOCAL_FILES_DOCUMENT_ROOT: /label-studio/data

      LABEL_STUDIO_USERNAME: ${LABEL_STUDIO_USERNAME}
      LABEL_STUDIO_PASSWORD: ${LABEL_STUDIO_PASSWORD}
      LABEL_STUDIO_USER_TOKEN: ${LABEL_STUDIO_USER_TOKEN}
      LABEL_STUDIO_ENABLE_LEGACY_API_TOKEN: "true"

  ml-backend:
    build: ./ml-backend
    container_name: ml-backend
    restart: unless-stopped
    expose:
      - "9090"
    volumes:
      - ./ml-backend/weights:/app/weights
    environment:
      CONFIG_PATH: /app/config.yaml
      LABEL_STUDIO_URL: http://label-studio:8080
      LABEL_STUDIO_API_KEY: ${LABEL_STUDIO_USER_TOKEN}
    depends_on:
      - label-studio

  caddy:
    image: caddy:2
    container_name: caddy
    restart: unless-stopped
    depends_on:
      - label-studio
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config

volumes:
  caddy_data:
  caddy_config: